import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.planeswalkerarchive.app',
  appName: "The Planeswalker's Archive",
  webDir: 'public'
};

export default config;
